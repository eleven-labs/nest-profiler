---
title: nest-profiler-mongoose
description: Types and API of the Mongoose query collector.
seo:
  title: Mongoose collector API reference
  description: 'Complete API reference for @eleven-labs/nest-profiler-mongoose: the types and options of the Mongoose query collector that records collection, operation, duration and result count per query.'
---

## MongooseCollectorModuleOptions

Options passed to `MongooseCollectorModule.forRoot()`.

<AutoTypeTable
  path="packages/nest-profiler-mongoose/src/mongoose-collector.module.ts"
  name="MongooseCollectorModuleOptions"
/>

## MongooseQueryEntry

One entry per Mongoose query or aggregation executed during a request.

<AutoTypeTable
  path="packages/nest-profiler-mongoose/src/mongoose-collector.interface.ts"
  name="MongooseQueryEntry"
/>

## Public exports

```ts
import { MongooseCollectorModule } from '@eleven-labs/nest-profiler-mongoose';
import { MongooseCollector } from '@eleven-labs/nest-profiler-mongoose';
import { MongooseSchemaCollectorModule } from '@eleven-labs/nest-profiler-mongoose';
import { MongooseSchemaCollector } from '@eleven-labs/nest-profiler-mongoose';

import type {
  MongooseCollectorModuleOptions,
  MongooseCollectorModuleAsyncOptions,
  MongooseSchemaCollectorModuleOptions,
  MongooseSchemaCollectorModuleAsyncOptions,
  MongooseQueryEntry,
} from '@eleven-labs/nest-profiler-mongoose';
```

## Setup

```ts title="profiling/profiling.module.ts"
// In the dev-only profiling bundle's imports:
MongooseCollectorModule.forRoot({
  slowThreshold: 50, // ms — queries at/above this are tagged 'slow'
});
```

**Prerequisite:** `MongooseModule.forRoot()` must be registered in the application (provides `Connection` globally via `@InjectConnection()`). The collector resolves it by token, so it needs no wiring in the modules that use `@nestjs/mongoose`; pass `connectionName` for a named connection.

> `ProfilingModule` is the dev-only bundle loaded by `main-dev.ts` — see [Enabling and disabling the profiler](/docs/packages/nest-profiler/configuration#recommended-install-it-as-a-dev-dependency). If the profiler is installed as a production dependency behind the [runtime gate](/docs/packages/nest-profiler/configuration#when-production-code-calls-the-profiler-conditionalmodule), wrap the same call in `ConditionalModule.registerWhen(..., isProfilerEnabled)`.

## Async setup

Drive the options from `ConfigService` (or any provider) with `forRootAsync()`, registered in the same bundle. It resolves option _values_ only — for the `enabled` flag and when to prefer `forRoot`, see [Enabling and disabling the profiler](/docs/packages/nest-profiler/configuration#enabling-and-disabling-the-profiler).

```ts
import { ConfigService } from '@nestjs/config';
import { MongooseCollectorModule } from '@eleven-labs/nest-profiler-mongoose';

MongooseCollectorModule.forRootAsync({
  inject: [ConfigService],
  useFactory: (config: ConfigService) => ({
    slowThreshold: config.get<number>('PROFILER_SLOW_QUERY_MS') ?? 50,
  }),
});
```
