---
title: nest-profiler-typeorm
description: Types and API of the TypeORM query collector.
seo:
  title: TypeORM collector API reference
  description: "Complete API reference for @eleven-labs/nest-profiler-typeorm: the types and options of the TypeORM query collector that records SQL, duration and slow queries in the profiler's Database panel."
---

## TypeOrmCollectorModuleOptions

Options passed to `TypeOrmCollectorModule.forRoot()`.

<AutoTypeTable
  path="packages/nest-profiler-typeorm/src/typeorm-collector.module.ts"
  name="TypeOrmCollectorModuleOptions"
/>

## QueryEntry

One entry per SQL query executed during a request.

<AutoTypeTable
  path="packages/nest-profiler/src/collectors/sql/sql-query.interface.ts"
  name="QueryEntry"
/>

## QueryType

```ts
type QueryType = 'SELECT' | 'INSERT' | 'UPDATE' | 'DELETE' | 'OTHER';
```

Detected from the first keyword of the SQL string.

## Public exports

```ts
import { TypeOrmCollectorModule } from '@eleven-labs/nest-profiler-typeorm';
import { TypeOrmCollector } from '@eleven-labs/nest-profiler-typeorm';
import { TypeOrmSchemaCollectorModule } from '@eleven-labs/nest-profiler-typeorm';
import { TypeOrmSchemaCollector } from '@eleven-labs/nest-profiler-typeorm';

import type {
  TypeOrmCollectorModuleOptions,
  TypeOrmCollectorModuleAsyncOptions,
  TypeOrmSchemaCollectorModuleOptions,
  TypeOrmSchemaCollectorModuleAsyncOptions,
  QueryEntry,
  QueryType,
} from '@eleven-labs/nest-profiler-typeorm';
```

## Setup

```ts title="profiling/profiling.module.ts"
// In the dev-only profiling bundle's imports:
TypeOrmCollectorModule.forRoot({
  slowThreshold: 50, // ms — queries at/above this are tagged 'slow'
});
```

**Prerequisite:** `TypeOrmModule.forRoot()` must be registered in the application (provides `DataSource` globally via `@InjectDataSource()`). The collector resolves it by token, so it needs no wiring in the module that uses TypeORM; pass `connectionName` for a named DataSource.

> `ProfilingModule` is the dev-only bundle loaded by `main-dev.ts` — see [Enabling and disabling the profiler](/docs/packages/nest-profiler/configuration#recommended-install-it-as-a-dev-dependency). If the profiler is installed as a production dependency behind the [runtime gate](/docs/packages/nest-profiler/configuration#when-production-code-calls-the-profiler-conditionalmodule), wrap the same call in `ConditionalModule.registerWhen(..., isProfilerEnabled)`.

## Async setup

Drive the options from `ConfigService` (or any provider) with `forRootAsync()`, registered in the same bundle. It resolves option _values_ only — for the `enabled` flag and when to prefer `forRoot`, see [Enabling and disabling the profiler](/docs/packages/nest-profiler/configuration#enabling-and-disabling-the-profiler).

```ts
import { ConfigService } from '@nestjs/config';
import { TypeOrmCollectorModule } from '@eleven-labs/nest-profiler-typeorm';

TypeOrmCollectorModule.forRootAsync({
  inject: [ConfigService],
  useFactory: (config: ConfigService) => ({
    slowThreshold: config.get<number>('PROFILER_SLOW_QUERY_MS') ?? 50,
  }),
});
```
