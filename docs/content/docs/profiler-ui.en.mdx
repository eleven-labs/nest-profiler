---
title: Profiler UI
description: A visual tour of the profiler web interface - the profiles list, the built-in tabs, and every collector panel.
seo:
  title: 'Web UI tour: panels & tabs'
---

Open `/_profiler` to browse the list of captured profiles, then click any row to open its detail view. The detail view is organized into **built-in tabs** (request, response, performance, logs, exceptions) plus **one tab per active collector**. The screenshots below come from the [example application](/docs/example-api) with every collector enabled.

## Profiles list

The `/_profiler` page groups captured profiles into a separate table per entrypoint kind (HTTP, GraphQL, Commands…). Each table has its own filter bar above it - the universal filters (search, duration, exceptions), the HTTP-status filters (status, status class) shown only on the kinds that carry an HTTP response (HTTP, GraphQL), and filters specific to that kind (e.g. HTTP **method**, RabbitMQ **delivery**/**exchange**/**handler**) - so each list is filtered independently. Global panels such as **Config** apply across the whole page.

![Recent profiles list with filters and the global Config panel](/screenshots/profiler/profiles-list.png)

## GraphQL operations

Requires [`@eleven-labs/nest-profiler-graphql`](/docs/packages/nest-profiler-graphql) - see the [GraphQL support section](/docs/getting-started#graphql-support) in Getting started.

GraphQL operations appear in their own **GraphQL** table (separate from REST requests), each row showing the operation type (**QUERY**, **MUTATION**, or **SUBSCRIPTION**) and the operation or field name. Its filter bar adds an **Operation** filter to narrow the list by operation type.

![Profiles list showing the GraphQL table with QUERY and MUTATION operations alongside their status](/screenshots/profiler/graphql-list.png)

Opening an operation lands on its dedicated **GraphQL** detail tab, displaying:

- **Operation type** badge (Query / Mutation / Subscription)
- **Operation name** if present in the request
- **Field name** - the entry-point resolver
- **Query** - syntax-highlighted GraphQL document
- **Variables** - the variables object, syntax-highlighted as JSON
- **Response** - the `{ data, errors }` envelope returned to the client
- **Request headers** - the underlying HTTP headers

![GraphQL detail tab with operation type, name, syntax-highlighted query and variables (Query)](/screenshots/profiler/graphql-request.png)

![GraphQL detail tab for a Mutation with input variables](/screenshots/profiler/graphql-mutation.png)

GraphQL-level errors (schema validation failures, resolver errors returned in `response.body.errors`) appear in the **Exceptions** tab with an amber `GraphQLError` badge, visually distinct from NestJS runtime exceptions which use a red badge.

![Exceptions tab showing an amber GraphQLError badge with validation error message and location](/screenshots/profiler/graphql-error.png)

## Built-in tabs

The **Request** tab details the method, URL, headers, and collected body.

![Request tab showing HTTP request details](/screenshots/profiler/request.png)

The **Response** tab shows the status, response headers, and body returned by the handler.

![Response tab showing status, headers, and JSON body](/screenshots/profiler/response.png)

The **Performance** tab groups request duration, memory, and timestamps.

![Performance tab with duration, memory, and request timeline](/screenshots/profiler/performance.png)

The **Logs** tab lists entries captured through `profilerService.createLogger()`. Each row shows the message first - with structured payloads rendered as a JSON block under it - then the logger context name. See the [Log capture](/docs/packages/nest-profiler/logs) page for the supported logger conventions.

![Logs tab with one log entry captured during the request](/screenshots/profiler/logs.png)

The **Exceptions** tab highlights the exception thrown by the `/error` endpoint.

![Exceptions tab displaying a captured BadRequestException](/screenshots/profiler/exceptions.png)

## Collectors

The **Timeline** collector displays spans created with `startSpan()` as synchronized bars.

![Timeline tab showing slow.step.fetch, slow.step.process, and slow.step.serialize spans](/screenshots/profiler/timeline.png)

The **Database** collector shows SQL queries (TypeORM or MikroORM) with their SQL type and duration.

![Database tab showing a profiled SQL SELECT query](/screenshots/profiler/database.png)

The **MongoDB** collector (requires [`@eleven-labs/nest-profiler-mongoose`](/docs/packages/nest-profiler-mongoose)) shows Mongoose queries and aggregations with their operation, collection, duration, and result count.

![MongoDB tab showing a profiled Mongoose query with collection, operation and duration](/screenshots/profiler/mongodb.png)

The **HTTP Client** collector shows outgoing requests triggered by `HttpService`.

![HTTP Client tab showing a GET call to JSONPlaceholder](/screenshots/profiler/http-client.png)

The **Cache** collector shows `GET_HIT`, `GET_MISS`, `SET` operations and the hit rate.

![Cache tab showing a GET_HIT and a 100 percent hit rate](/screenshots/profiler/cache.png)

The **Command** collector (requires [`@eleven-labs/nest-profiler-commander`](/docs/packages/nest-profiler-commander)) profiles `nest-commander` CLI runs. With file storage, command profiles appear in the list with a **CLI** method badge and the command line in place of the URL; the **Command** tab details the command name, arguments, options, and exit code.

![Command tab showing a profiled nest-commander run with arguments, options and exit code](/screenshots/profiler/command.png)

The **Security** collector shows the authenticated user, roles, and decoded JWT claims.

![Security tab showing an authenticated demo_user with the admin role](/screenshots/profiler/security.png)

The **Validator** collector shows validated DTOs and failing `class-validator` constraints.

![Validator tab showing two validation violations on CreatePostDto](/screenshots/profiler/validator.png)

The **Config** collector is global: it appears on the profiles list and exposes flattened configuration with masked secrets.

![Global Config panel showing app and database keys with database.password masked](/screenshots/profiler/config.png)
