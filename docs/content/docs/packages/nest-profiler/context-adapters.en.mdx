---
title: Custom entrypoints & adapters
description: Profile any non-HTTP entrypoint (gRPC, Kafka, WebSockets, CLI commands…) - produce the profile with IContextAdapter and render it with registerEntrypointType, no core changes required.
seo:
  title: Profile gRPC, Kafka & CLI entrypoints
  description: 'Profile any non-HTTP entrypoint — gRPC, Kafka, WebSockets or CLI commands — by producing the profile with IContextAdapter and rendering it through registerEntrypointType, with no changes to the core.'
---

<include>../../../../../packages/nest-profiler/docs/context-adapters.md</include>
