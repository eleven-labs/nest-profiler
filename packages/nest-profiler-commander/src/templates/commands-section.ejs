<div class="rounded-xl border border-line overflow-hidden bg-surface">
  <table class="w-full">
    <thead>
      <tr class="bg-surface-muted border-b border-line">
        <th class="text-left py-3 px-4 text-foreground-muted font-medium text-2xs uppercase tracking-widest">Token</th>
        <th class="text-left py-3 px-4 text-foreground-muted font-medium text-2xs uppercase tracking-widest">Command</th>
        <th class="text-left py-3 px-4 text-foreground-muted font-medium text-2xs uppercase tracking-widest">Arguments</th>
        <th class="text-left py-3 px-4 text-foreground-muted font-medium text-2xs uppercase tracking-widest">Status</th>
        <th class="text-left py-3 px-4 text-foreground-muted font-medium text-2xs uppercase tracking-widest">Duration</th>
        <th class="text-left py-3 px-4 text-foreground-muted font-medium text-2xs uppercase tracking-widest">Time</th>
      </tr>
    </thead>
    <tbody class="divide-y divide-line-subtle">
      <% for (const p of profiles) { %>
        <%
          const cmd = p.entrypoint.data;
          const dur = p.performance.duration;
          const durClass = dur === undefined ? 'dur-none'
            : dur < 100  ? 'dur-fast'
            : dur < 500  ? 'dur-medium'
            : 'dur-slow';
        %>
        <tr class="hover:bg-surface-muted transition-colors">
          <td class="py-2.5 px-4">
            <a href="<%= link(profilerPath + '/' + p.token) %>"
               class="text-nest hover:text-nest-light transition-colors font-mono font-medium text-xs">
              <%= p.token.slice(0, 8) %>
            </a>
          </td>
          <td class="py-2.5 px-4">
            <div class="flex items-center gap-1.5">
              <span class="px-2 py-0.5 rounded text-2xs font-bold tracking-wide badge-default">CLI</span>
              <code class="text-foreground-secondary text-xs"><%= cmd.name %></code>
            </div>
          </td>
          <td class="py-2.5 px-4 text-foreground-faint text-xs max-w-xs truncate" title="<%= cmd.arguments.join(' ') %>">
            <%= cmd.arguments.length ? cmd.arguments.join(' ') : '—' %>
          </td>
          <td class="py-2.5 px-4">
            <span class="px-2 py-0.5 rounded text-2xs font-bold tracking-wide <%= cmd.success ? 'badge-2xx' : 'badge-5xx' %>">
              <%= cmd.success ? 'OK' : 'FAILED' %>
            </span>
            <span class="text-foreground-faint text-2xs ml-1 tabular-nums">exit <%= cmd.exitCode %></span>
          </td>
          <td class="py-2.5 px-4 text-xs tabular-nums">
            <span class="<%= durClass %>"><%= dur !== undefined ? dur + 'ms' : '—' %></span>
          </td>
          <td class="py-2.5 px-4 text-foreground-faint text-xs tabular-nums"><%= isoDate(p.createdAt) %></td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>
